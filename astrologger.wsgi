import sys

# Add your application directory to the system path
sys.path.insert(0, "/var/www/astrologger")

# Import the 'app' object from your app.py file
from AstroLogger import app as application

